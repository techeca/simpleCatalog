import { AnimatePresence, motion } from "framer-motion";
import { useId } from "react";

export default function CustomCard({card, id, setActive}){
    //const id = useId()

    return(
        
    )
}